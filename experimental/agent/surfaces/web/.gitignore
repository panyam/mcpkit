# Source maps are dev aids regenerated by `npm run build`; the committed bundle
# (static/*.js) is what Go embeds, the maps are not needed in the repo.
static/*.js.map

# compiled serve binary (go run / go build output)
/agentweb
