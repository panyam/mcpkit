// Placeholder frontend bundle for agentweb (E3, issue 1196). The real DockView +
// Solid island app, built with esbuild and consuming the Connect-Web client
// generated from protos/, lands in E4 (issue 1197). This file exists so the
// /static route has something to serve and the serve binary's asset check
// passes.
console.log("agentweb placeholder bundle — the DockView + Solid frontend is E4 (issue 1197)");
