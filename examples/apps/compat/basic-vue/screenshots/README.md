# Screenshots — basic-vue

Placeholder paths referenced from `../README.md`. Capture by running:

```bash
RENDERER=basic-host make demo-app EXAMPLE=basic-server-vue
```

| File | What to capture |
|---|---|
| `01-get-time.png` | basic-host with the vue App iframe loaded; iframe shows the rendered current-time and a button to recall the tool |
