# Screenshots — basic-svelte

Placeholder paths referenced from `../README.md`. Capture by running:

```bash
RENDERER=basic-host make demo-app EXAMPLE=basic-server-svelte
```

| File | What to capture |
|---|---|
| `01-get-time.png` | basic-host with the svelte App iframe loaded; iframe shows the rendered current-time and a button to recall the tool |
