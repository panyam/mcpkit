# Screenshots — basic-vanillajs

Placeholder paths referenced from `../README.md`. Capture by running:

```bash
just demo-app EXAMPLE=basic-vanillajs
```

| File | What to capture |
|---|---|
| `01-get-time.png` | basic-host with the vanilla-JS App iframe loaded; iframe shows the rendered current-time and a button to recall the tool |
