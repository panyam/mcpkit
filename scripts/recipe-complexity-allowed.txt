# C8 baseline — recipes allowed to hold a shell script instead of calling one.
#
# It is EMPTY, and that is the goal state. The sweep that introduced C8 took it
# from 66 entries to none.
#
# Adding a line means a new inline script landed and someone decided to keep it.
# That needs a reason in the PR, not a silent append. The checker also fails on
# an entry that no longer violates, so the list cannot rot in the other
# direction the way an --update-baseline flag lets a baseline rot.
#
# Format: <path from repo root>::<recipe>
# Current offenders, if you are repopulating after a regression:
#   scripts/check_recipe_complexity.py --list
