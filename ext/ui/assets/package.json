{
  "name": "@mcpkit/app-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-agnostic postMessage bridge for MCP Apps (iframe side)",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
